<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easytrip.mappers.WebCarouselMapper">

	<!--实体映射-->
	<resultMap id="base_result_map" type="com.easytrip.entity.po.WebCarousel">
		<!--轮播图id-->
		<result column="carousel_id" property="carouselId"  />
		<!--图片-->
		<result column="img_path" property="imgPath"  />
		<!--0:分享文章 1:景点 2:酒店住宿 3 外部连接-->
		<result column="object_type" property="objectType"  />
		<!--object_id-->
		<result column="object_id" property="objectId"  />
		<!--外部连接-->
		<result column="outer_link" property="outerLink"  />
		<!--排序-->
		<result column="sort" property="sort"  />
	</resultMap>


	<!-- 通用查询结果列-->
	<sql id="base_column_list">
		 carousel_id,img_path,object_type,object_id,outer_link,
		 sort
	</sql>

	<sql id="base_condition_filed">
		<if test="query.carouselId != null">
			 and  carousel_id = #{query.carouselId}
		</if>
		<if test="query.imgPath != null and query.imgPath!=''">
			 and  img_path = #{query.imgPath}
		</if>
		<if test="query.objectType != null">
			 and  object_type = #{query.objectType}
		</if>
		<if test="query.objectId != null and query.objectId!=''">
			 and  object_id = #{query.objectId}
		</if>
		<if test="query.outerLink != null and query.outerLink!=''">
			 and  outer_link = #{query.outerLink}
		</if>
		<if test="query.sort != null">
			 and  sort = #{query.sort}
		</if>
	</sql>

	<!-- 通用查询条件列-->
	<sql id="query_condition">
		<where>
			<include refid="base_condition_filed" />
			<if test="query.imgPathFuzzy!= null  and query.imgPathFuzzy!=''">
				 and  img_path like concat('%', #{query.imgPathFuzzy}, '%')
			</if>
			<if test="query.objectIdFuzzy!= null  and query.objectIdFuzzy!=''">
				 and  object_id like concat('%', #{query.objectIdFuzzy}, '%')
			</if>
			<if test="query.outerLinkFuzzy!= null  and query.outerLinkFuzzy!=''">
				 and  outer_link like concat('%', #{query.outerLinkFuzzy}, '%')
			</if>
		</where>
	</sql>

	<!-- 查询集合-->
	<select id="selectList" resultMap="base_result_map" >
		 SELECT <include refid="base_column_list" /> FROM web_carousel <include refid="query_condition" />
		 <if test="query.orderBy!=null">
			 order by ${query.orderBy}
		 </if>
		 <if test="query.simplePage!=null">
			 limit #{query.simplePage.start},#{query.simplePage.end}
		 </if>
	</select>

	<!-- 查询数量-->
	<select id="selectCount" resultType="java.lang.Integer" >
		 SELECT count(1) FROM web_carousel <include refid="query_condition" />
	</select>

	<!-- 插入 （匹配有值的字段）-->
	<insert id="insert" parameterType="com.easytrip.entity.po.WebCarousel">
		 INSERT INTO web_carousel
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="bean.carouselId != null">
				 carousel_id,
			</if>
			<if test="bean.imgPath != null">
				 img_path,
			</if>
			<if test="bean.objectType != null">
				 object_type,
			</if>
			<if test="bean.objectId != null">
				 object_id,
			</if>
			<if test="bean.outerLink != null">
				 outer_link,
			</if>
			<if test="bean.sort != null">
				 sort,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="bean.carouselId!=null">
				 #{bean.carouselId},
			</if>
			<if test="bean.imgPath!=null">
				 #{bean.imgPath},
			</if>
			<if test="bean.objectType!=null">
				 #{bean.objectType},
			</if>
			<if test="bean.objectId!=null">
				 #{bean.objectId},
			</if>
			<if test="bean.outerLink!=null">
				 #{bean.outerLink},
			</if>
			<if test="bean.sort!=null">
				 #{bean.sort},
			</if>
		 </trim>
	</insert>

	<!-- 插入或者更新 （匹配有值的字段）-->
	<insert id="insertOrUpdate" parameterType="com.easytrip.entity.po.WebCarousel">
		 INSERT INTO web_carousel
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.carouselId != null">
				 carousel_id,
			</if>
			<if test="bean.imgPath != null">
				 img_path,
			</if>
			<if test="bean.objectType != null">
				 object_type,
			</if>
			<if test="bean.objectId != null">
				 object_id,
			</if>
			<if test="bean.outerLink != null">
				 outer_link,
			</if>
			<if test="bean.sort != null">
				 sort,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.carouselId!=null">
				 #{bean.carouselId},
			</if>
			<if test="bean.imgPath!=null">
				 #{bean.imgPath},
			</if>
			<if test="bean.objectType!=null">
				 #{bean.objectType},
			</if>
			<if test="bean.objectId!=null">
				 #{bean.objectId},
			</if>
			<if test="bean.outerLink!=null">
				 #{bean.outerLink},
			</if>
			<if test="bean.sort!=null">
				 #{bean.sort},
			</if>
		 </trim>
		 on DUPLICATE key update 
		 <trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.carouselId!=null">
				 carousel_id = VALUES(carousel_id),
			</if>
			<if test="bean.imgPath!=null">
				 img_path = VALUES(img_path),
			</if>
			<if test="bean.objectType!=null">
				 object_type = VALUES(object_type),
			</if>
			<if test="bean.objectId!=null">
				 object_id = VALUES(object_id),
			</if>
			<if test="bean.outerLink!=null">
				 outer_link = VALUES(outer_link),
			</if>
			<if test="bean.sort!=null">
				 sort = VALUES(sort),
			</if>
		 </trim>
	</insert>

	<!-- 添加 （批量插入）-->
	<insert id="insertBatch" parameterType="com.easytrip.entity.po.WebCarousel">
		 INSERT INTO web_carousel(
			 carousel_id,
			 img_path,
			 object_type,
			 object_id,
			 outer_link,
			 sort
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.carouselId},
				 #{item.imgPath},
				 #{item.objectType},
				 #{item.objectId},
				 #{item.outerLink},
				 #{item.sort}
			 )
		 </foreach>
	</insert>

	<!-- 批量新增修改 （批量插入）-->
	<insert id="insertOrUpdateBatch" parameterType="com.easytrip.entity.po.WebCarousel">
		 INSERT INTO web_carousel(
			 carousel_id,
			 img_path,
			 object_type,
			 object_id,
			 outer_link,
			 sort
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.carouselId},
				 #{item.imgPath},
				 #{item.objectType},
				 #{item.objectId},
				 #{item.outerLink},
				 #{item.sort}
			 )
		 </foreach>
			on DUPLICATE key update 
			carousel_id = VALUES(carousel_id),
			img_path = VALUES(img_path),
			object_type = VALUES(object_type),
			object_id = VALUES(object_id),
			outer_link = VALUES(outer_link),
			sort = VALUES(sort)
	</insert>

	<!--多条件修改-->
	<update id="updateByParam" parameterType="com.easytrip.entity.query.WebCarouselQuery">
		 UPDATE web_carousel
 		 <set> 
			<if test="bean.carouselId != null">
				 carousel_id = #{bean.carouselId},
			</if>
			<if test="bean.imgPath != null">
				 img_path = #{bean.imgPath},
			</if>
			<if test="bean.objectType != null">
				 object_type = #{bean.objectType},
			</if>
			<if test="bean.objectId != null">
				 object_id = #{bean.objectId},
			</if>
			<if test="bean.outerLink != null">
				 outer_link = #{bean.outerLink},
			</if>
			<if test="bean.sort != null">
				 sort = #{bean.sort},
			</if>
 		 </set>
 		 <include refid="query_condition" />
	</update>

	<!--多条件删除-->
	<delete id="deleteByParam">
		 delete from web_carousel
 		 <include refid="query_condition" />
	</delete>

	<!-- 根据CarouselId修改-->
	<update id="updateByCarouselId" parameterType="com.easytrip.entity.po.WebCarousel">
		 UPDATE web_carousel
 		 <set> 
			<if test="bean.imgPath != null">
				 img_path = #{bean.imgPath},
			</if>
			<if test="bean.objectType != null">
				 object_type = #{bean.objectType},
			</if>
			<if test="bean.objectId != null">
				 object_id = #{bean.objectId},
			</if>
			<if test="bean.outerLink != null">
				 outer_link = #{bean.outerLink},
			</if>
			<if test="bean.sort != null">
				 sort = #{bean.sort},
			</if>
 		 </set>
 		 where carousel_id=#{carouselId}
	</update>

	<!-- 根据CarouselId删除-->
	<delete id="deleteByCarouselId">
		delete from web_carousel where carousel_id=#{carouselId}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectByCarouselId" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from web_carousel where carousel_id=#{carouselId}
	</select>
	<select id="selectMaxSort" resultType="java.lang.Integer">
		SELECT MAX(sort)
		FROM web_carousel;
	</select>
</mapper>