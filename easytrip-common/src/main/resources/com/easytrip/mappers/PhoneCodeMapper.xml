<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easytrip.mappers.PhoneCodeMapper">

	<!--实体映射-->
	<resultMap id="base_result_map" type="com.easytrip.entity.po.PhoneCode">
		<!--验证码id-->
		<id column="id" property="id"  />
		<!--手机号-->
		<result column="phoneNumber" property="phonenumber"  />
		<!--验证码-->
		<result column="code" property="code"  />
		<!--创建时间-->
		<result column="create_time" property="createTime"  />
		<!--0:未使用 1:已使用-->
		<result column="status" property="status"  />
		<result column="type" property="type"  />
	</resultMap>


	<!-- 通用查询结果列-->
	<sql id="base_column_list">
		 id,phoneNumber,code,create_time,status

	</sql>

	<sql id="base_condition_filed">
		<if test="query.id != null">
			 and  id = #{query.id}
		</if>
		<if test="query.phonenumber != null and query.phonenumber!=''">
			 and  phoneNumber = #{query.phonenumber}
		</if>
		<if test="query.code != null and query.code!=''">
			 and  code = #{query.code}
		</if>
		<if test="query.createTime != null and query.createTime!=''">
			 <![CDATA[ and  create_time=str_to_date(#{query.createTime}, '%Y-%m-%d') ]]>
		</if>
		<if test="query.status != null">
			 and  status = #{query.status}
		</if>
	</sql>

	<!-- 通用查询条件列-->
	<sql id="query_condition">
		<where>
			<include refid="base_condition_filed" />
			<if test="query.phonenumberFuzzy!= null  and query.phonenumberFuzzy!=''">
				 and  phoneNumber like concat('%', #{query.phonenumberFuzzy}, '%')
			</if>
			<if test="query.codeFuzzy!= null  and query.codeFuzzy!=''">
				 and  code like concat('%', #{query.codeFuzzy}, '%')
			</if>
			<if test="query.createTimeStart!= null and query.createTimeStart!=''">
				 <![CDATA[ and  create_time>=str_to_date(#{query.createTimeStart}, '%Y-%m-%d') ]]>
			</if>
			<if test="query.createTimeEnd!= null and query.createTimeEnd!=''">
				 <![CDATA[ and  create_time< date_sub(str_to_date(#{query.createTimeEnd},'%Y-%m-%d'),interval -1 day) ]]>
			</if>
		</where>
	</sql>

	<!-- 查询集合-->
	<select id="selectList" resultMap="base_result_map" >
		 SELECT <include refid="base_column_list" /> FROM phone_code <include refid="query_condition" />
		 <if test="query.orderBy!=null">
			 order by ${query.orderBy}
		 </if>
		 <if test="query.simplePage!=null">
			 limit #{query.simplePage.start},#{query.simplePage.end}
		 </if>
	</select>

	<!-- 查询数量-->
	<select id="selectCount" resultType="java.lang.Integer" >
		 SELECT count(1) FROM phone_code <include refid="query_condition" />
	</select>

	<!-- 插入 （匹配有值的字段）-->
	<insert id="insert" parameterType="com.easytrip.entity.po.PhoneCode">
		<selectKey keyProperty="bean.id" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
		 INSERT INTO phone_code
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="bean.phonenumber != null">
				 phoneNumber,
			</if>
			<if test="bean.code != null">
				 code,
			</if>
			<if test="bean.createTime != null">
				 create_time,
			</if>
			<if test="bean.status != null">
				 status,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="bean.phonenumber!=null">
				 #{bean.phonenumber},
			</if>
			<if test="bean.code!=null">
				 #{bean.code},
			</if>
			<if test="bean.createTime!=null">
				 #{bean.createTime},
			</if>
			<if test="bean.status!=null">
				 #{bean.status},
			</if>
		 </trim>
	</insert>

	<!-- 插入或者更新 （匹配有值的字段）-->
	<insert id="insertOrUpdate" parameterType="com.easytrip.entity.po.PhoneCode">
		 INSERT INTO phone_code
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				 id,
			</if>
			<if test="bean.phonenumber != null">
				 phoneNumber,
			</if>
			<if test="bean.code != null">
				 code,
			</if>
			<if test="bean.createTime != null">
				 create_time,
			</if>
			<if test="bean.status != null">
				 status,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.id!=null">
				 #{bean.id},
			</if>
			<if test="bean.phonenumber!=null">
				 #{bean.phonenumber},
			</if>
			<if test="bean.code!=null">
				 #{bean.code},
			</if>
			<if test="bean.createTime!=null">
				 #{bean.createTime},
			</if>
			<if test="bean.status!=null">
				 #{bean.status},
			</if>
		 </trim>
		 on DUPLICATE key update 
		 <trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.phonenumber!=null">
				 phoneNumber = VALUES(phoneNumber),
			</if>
			<if test="bean.code!=null">
				 code = VALUES(code),
			</if>
			<if test="bean.createTime!=null">
				 create_time = VALUES(create_time),
			</if>
			<if test="bean.status!=null">
				 status = VALUES(status),
			</if>
		 </trim>
	</insert>

	<!-- 添加 （批量插入）-->
	<insert id="insertBatch" parameterType="com.easytrip.entity.po.PhoneCode" useGeneratedKeys="true" keyProperty="id">
		 INSERT INTO phone_code(
			 phoneNumber,
			 code,
			 create_time,
			 status
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.phonenumber},
				 #{item.code},
				 #{item.createTime},
				 #{item.status}
			 )
		 </foreach>
	</insert>

	<!-- 批量新增修改 （批量插入）-->
	<insert id="insertOrUpdateBatch" parameterType="com.easytrip.entity.po.PhoneCode">
		 INSERT INTO phone_code(
			 phoneNumber,
			 code,
			 create_time,
			 status
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.phonenumber},
				 #{item.code},
				 #{item.createTime},
				 #{item.status}
			 )
		 </foreach>
			on DUPLICATE key update 
			phoneNumber = VALUES(phoneNumber),
			code = VALUES(code),
			create_time = VALUES(create_time),
			status = VALUES(status)
	</insert>

	<!--多条件修改-->
	<update id="updateByParam" parameterType="com.easytrip.entity.query.PhoneCodeQuery">
		 UPDATE phone_code
 		 <set> 
			<if test="bean.phonenumber != null">
				 phoneNumber = #{bean.phonenumber},
			</if>
			<if test="bean.code != null">
				 code = #{bean.code},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
			<if test="bean.status != null">
				 status = #{bean.status},
			</if>
 		 </set>
 		 <include refid="query_condition" />
	</update>

	<!--多条件删除-->
	<delete id="deleteByParam">
		 delete from phone_code
 		 <include refid="query_condition" />
	</delete>

	<!-- 根据Id修改-->
	<update id="updateById" parameterType="com.easytrip.entity.po.PhoneCode">
		 UPDATE phone_code
 		 <set> 
			<if test="bean.phonenumber != null">
				 phoneNumber = #{bean.phonenumber},
			</if>
			<if test="bean.code != null">
				 code = #{bean.code},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
			<if test="bean.status != null">
				 status = #{bean.status},
			</if>
 		 </set>
 		 where id=#{id}
	</update>

	<!-- 根据Id删除-->
	<delete id="deleteById">
		delete from phone_code where id=#{id}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectById" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from phone_code where id=#{id}
	</select>

</mapper>